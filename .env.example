# plugin-test-flaky environment
# Test plugin that intentionally crashes to test error handling.
# For local discovery mode, only PLUGIN_DATA_DIR is required.

# Required: Directory for plugin state storage
PLUGIN_DATA_DIR=/tmp/plugin-test-flaky
